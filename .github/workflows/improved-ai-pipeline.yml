name: AI Article Generation & Publishing Pipeline

on:
  schedule:
    - cron: '15 */8 * * *'  # 8時間ごと
  workflow_dispatch:

jobs:
  generate-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch full history for proper rebasing
          
      - name: Sync with latest changes
        run: |
          echo "🔄 Syncing with latest remote changes..."
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git pull origin main || echo "⚠️ No remote changes to pull"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          npm install -g @google/gemini-cli textlint textlint-rule-preset-japanese textlint-rule-preset-ja-technical-writing @textlint-ja/textlint-rule-preset-ai-writing
          pip install sentence-transformers scikit-learn numpy requests pillow
          mkdir -p _temp assets/images/posts

      - name: Generate topics and articles
        shell: bash
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          export GEMINI_API_KEY="$GEMINI_API_KEY"
          
          echo "🎯 Generating AI topics..."
          
          # GitHub Actions環境でのAPI key認証確認
          if [ -z "$GEMINI_API_KEY" ]; then
            echo "❌ GEMINI_API_KEY is not set. Please check GitHub Secrets configuration."
            exit 1
          fi
          
          # Gemini CLI with API key authentication
          echo "Testing Gemini API connection..."
          if ! gemini --version; then
            echo "❌ Gemini CLI not found or not working"
            exit 1
          fi
          
          
          
          # Stage 1: 3段階インテリジェント情報収集の実装
          echo "🎯 Stage 1: Multi-angle information gathering..."
          
          # Phase 1: 技術者・投資家向け基本情報収集
          SEARCH_PHASE_1="WebSearch: \$(date '+%Y年%m月%d日') AI業界 最新ニュース トレンド。AI業界で注目されている最新話題をWeb検索で調査し、以下の観点から包括的に情報収集：【技術者向け情報】技術仕様・性能ベンチマーク、API仕様・料金体系、既存モデルとの比較データ、実装・統合の技術的課題【投資家向け情報】市場規模・成長予測、競合他社の対応状況、収益モデル・価格戦略、企業の財務インパクト【ファクトチェック情報】公式発表・プレスリリース、決算資料・IR情報、第三者機関の評価・レポート、業界専門家のコメント。重要：具体的な数値データ、企業名、製品名、人名を必ず含め、5個の話題を番号付きでリストアップしてください。"
          
          echo "🔍 Phase 1: Basic information collection..."
          
          # Phase 1実行: 基本情報収集
          API_ATTEMPT=1
          echo "🔍 API attempt $API_ATTEMPT: Phase 1 multi-angle search"
          
          if gemini -m "gemini-2.5-flash" -p "$SEARCH_PHASE_1" > _temp/phase1_results.txt 2>_temp/gemini_error.log; then
            echo "🔍 Debug: Phase 1 API call completed successfully"
            echo "🔍 Debug: Response length: $(wc -c < _temp/phase1_results.txt) characters"
            echo "🔍 Debug: First 200 characters:"
            head -c 200 _temp/phase1_results.txt
            echo ""
            if ! grep -q "What would you like to work on\|How can I help\|What can I help you with" _temp/phase1_results.txt 2>/dev/null; then
              echo "✅ Phase 1 search successful"
              PHASE1_SUCCESS=true
            else
              echo "❌ Phase 1 returned generic response"
              PHASE1_SUCCESS=false
            fi
          else
            echo "❌ Phase 1 API call failed"
            echo "🔍 Debug: Error details:"
            cat _temp/gemini_error.log 2>/dev/null || echo "No error log available"
            PHASE1_SUCCESS=false
          fi
          
          # Phase 2: 市場・投資分析特化検索
          if [ "$PHASE1_SUCCESS" = "true" ]; then
            API_ATTEMPT=$((API_ATTEMPT + 1))
            echo "🔍 Phase 2: Market and investment analysis..."
            
            SEARCH_PHASE_2="WebSearch: \$(date '+%Y年%m月%d日') AI業界 市場分析 投資 資金調達 競合 シェア。AI関連の市場・競合分析に特化した追加調査：【市場データ】市場規模・成長率の具体的数値、地域別・セグメント別の動向、主要プレイヤーの市場シェア【投資・財務データ】関連企業の株価・時価総額、投資ラウンド・資金調達情報、売上・利益への影響予測【競合分析】直接競合の製品・戦略比較、差別化要因・競争優位性、今後の競争構造予測。重要：Phase 1で収集した情報を補完する詳細データを、具体的な数値・企業名・投資額を含めて5個の話題として番号付きでリストアップしてください。"
            
            if gemini -m "gemini-2.5-flash" -p "$SEARCH_PHASE_2" > _temp/phase2_results.txt 2>_temp/gemini_error2.log; then
              echo "✅ Phase 2 search successful"
              PHASE2_SUCCESS=true
            else
              echo "⚠️ Phase 2 search failed, proceeding with Phase 1 results"
              PHASE2_SUCCESS=false
            fi
          else
            echo "⚠️ Skipping Phase 2 due to Phase 1 failure"
            PHASE2_SUCCESS=false
          fi
          
          # Phase 3: 情報検証・統合
          if [ "$PHASE1_SUCCESS" = "true" ]; then
            API_ATTEMPT=$((API_ATTEMPT + 1))
            echo "🔍 Phase 3: Information verification and consolidation..."
            
            # Phase 1と2の結果を結合
            cat _temp/phase1_results.txt > _temp/combined_results.txt
            if [ "$PHASE2_SUCCESS" = "true" ]; then
              echo "" >> _temp/combined_results.txt
              cat _temp/phase2_results.txt >> _temp/combined_results.txt
            fi
            
            # Phase 3のプロンプトに結合データを直接組み込む
            echo "以下の3段階検索で収集された情報について、信頼性と整合性を検証し、投資家・技術者向けに整理してください。" > _temp/verification_prompt.txt
            echo "" >> _temp/verification_prompt.txt
            echo "【収集情報】" >> _temp/verification_prompt.txt
            cat _temp/combined_results.txt >> _temp/verification_prompt.txt
            echo "" >> _temp/verification_prompt.txt
            echo "【検証・整理要件】1.情報源の信頼性評価（公式発表>業界レポート>メディア報道）2.数値データの整合性チェック 3.矛盾する情報の指摘と解釈 4.投資家・技術者が求める重要情報の抽出【出力形式】最終的に5個の高品質な話題を番号付きでリストアップしてください。各話題は具体的な企業名・製品名・人名・数値データを含み、投資判断または技術選定に有用な情報で、信頼できる情報源に基づく内容。例:1.OpenAI GPT-5、企業向けAPI価格を従来比30%削減し月額500ドルで提供開始" >> _temp/verification_prompt.txt
            
            if cat _temp/verification_prompt.txt | gemini -m "gemini-2.5-flash" > _temp/topics.txt 2>_temp/gemini_error3.log; then
              echo "✅ Phase 3 verification successful"
              TOPIC_API_SUCCESS=true
            else
              echo "⚠️ Phase 3 failed, using Phase 1 results directly"
              cp _temp/phase1_results.txt _temp/topics.txt
              TOPIC_API_SUCCESS=true
            fi
          else
            echo "❌ All phases failed - stopping workflow"
            echo "🔍 Debug: Checking error logs..."
            echo "--- Phase 1 Error ---"
            cat _temp/gemini_error.log 2>/dev/null || echo "No Phase 1 error log"
            echo "🚫 Stopping workflow to prevent excessive API usage"
            exit 1
          fi
          
          echo "🔍 Final verified topics preview:"
          head -3 _temp/topics.txt
          
          # Topic generation completed successfully with one of the methods above
          
          echo "Generated topics:"
          head -15 _temp/topics.txt
          
          # Extract and filter duplicate topics
          ALL_GENERATED_TOPICS=$(grep -E "^[0-9]+\." _temp/topics.txt)
          echo "📝 Initial topics generated:"
          echo "$ALL_GENERATED_TOPICS"
          
          if [ -z "$ALL_GENERATED_TOPICS" ]; then
            echo "❌ No topics generated, exiting..."
            exit 1
          fi
          
          # Get existing titles for duplicate check (last 5 days)
          if ls _posts/*.md 1> /dev/null 2>&1; then
            find _posts -name "*.md" -mtime -5 -exec grep -h "^title:" {} \; 2>/dev/null | sed 's/^title: *["]*\|["]*$//g' > _temp/existing_titles.txt
          else
            touch _temp/existing_titles.txt
          fi
          
          echo "🔍 Filtering out duplicate topics..."
          TOPICS=""
          TOPIC_COUNT=0
          RETRY_COUNT=0
          MAX_RETRIES=3
          
          # Filter duplicates and get unique topics (reduced to 5 to stay within API limits)
          while IFS= read -r topic_line && [ $TOPIC_COUNT -lt 5 ]; do
            if [ -z "$topic_line" ]; then
              continue
            fi
            
            topic_title=$(printf '%s\n' "$topic_line" | sed 's/^[0-9]*\. *//')
            echo "🔍 Checking: $topic_title"
            
            # Simple keyword-based duplicate check
            is_duplicate=false
            if [ -s _temp/existing_titles.txt ]; then
              # Extract key words from topic (first 3 significant words)
              topic_keywords=$(echo "$topic_title" | sed 's/[()（）].*//g' | head -c 30)
              while IFS= read -r existing_title; do
                if [ -n "$existing_title" ] && echo "$existing_title" | grep -qi "$(echo "$topic_keywords" | cut -d' ' -f1-2)"; then
                  echo "⏭️ Duplicate detected: $topic_title (similar to existing: $existing_title)"
                  is_duplicate=true
                  break
                fi
              done < _temp/existing_titles.txt
            fi
            
            if [ "$is_duplicate" = false ]; then
              if [ -z "$TOPICS" ]; then
                TOPICS="$topic_line"
              else
                TOPICS="$TOPICS"$'\n'"$topic_line"
              fi
              TOPIC_COUNT=$((TOPIC_COUNT + 1))
              echo "✅ Added unique topic: $topic_title"
            fi
          done <<< "$ALL_GENERATED_TOPICS"
          
          # If we don't have enough unique topics, generate more
          # Disable additional topic generation to minimize API calls
          if false; then  # This entire block is disabled
            RETRY_COUNT=$((RETRY_COUNT + 1))
            echo "🔄 Need more topics ($TOPIC_COUNT/5). Retry $RETRY_COUNT/$MAX_RETRIES with different prompt..."
            
            # Generate additional topics with different search strategies
            case $RETRY_COUNT in
              1)
                ADDITIONAL_PROMPT="WebSearch: AI業界 スタートアップ 投資 資金調達 IPO M&A。AI関連スタートアップや投資動向に焦点を当てた最新話題を$(( 5 - TOPIC_COUNT + 2 ))個生成してください。具体的な企業名・金額・投資家名を含む話題を番号付きでリストアップ。"
                ;;
              2)
                ADDITIONAL_PROMPT="WebSearch: AI業界 研究 論文 学会 大学 研究機関。AI研究や学術論文、研究機関の最新成果に焦点を当てた話題を$(( 5 - TOPIC_COUNT + 2 ))個生成してください。具体的な研究者名・大学名・論文名を含む話題を番号付きでリストアップ。"
                ;;
              *)
                ADDITIONAL_PROMPT="WebSearch: AI業界 政府 政策 規制 法律 国際協力。AI関連の政府政策や規制、国際的な取り組みに焦点を当てた話題を$(( 5 - TOPIC_COUNT + 2 ))個生成してください。具体的な国名・政府機関名・政策名を含む話題を番号付きでリストアップ。"
                ;;
            esac
            
            if echo "$ADDITIONAL_PROMPT" | gemini -m "gemini-2.5-flash" > _temp/additional_topics.txt 2>/dev/null; then
              echo "Generated additional topics (stdin method)"
            elif gemini -m "gemini-2.5-flash" -p "$ADDITIONAL_PROMPT" > _temp/additional_topics.txt 2>/dev/null; then
              echo "Generated additional topics (-p method)"
            elif gemini -m "gemini-2.5-flash" --prompt "$ADDITIONAL_PROMPT" > _temp/additional_topics.txt 2>/dev/null; then
              ADDITIONAL_TOPICS=$(grep -E "^[0-9]+\." _temp/additional_topics.txt)
              echo "Generated additional topics:"
              echo "$ADDITIONAL_TOPICS"
              
              # Filter new topics for duplicates
              while IFS= read -r topic_line && [ $TOPIC_COUNT -lt 5 ]; do
                if [ -z "$topic_line" ]; then
                  continue
                fi
                
                topic_title=$(printf '%s\n' "$topic_line" | sed 's/^[0-9]*\. *//')
                
                # Check against existing titles and already selected topics
                is_duplicate=false
                topic_keywords=$(echo "$topic_title" | sed 's/[()（）].*//g' | head -c 30)
                
                # Check against existing posts
                if [ -s _temp/existing_titles.txt ]; then
                  while IFS= read -r existing_title; do
                    if [ -n "$existing_title" ] && echo "$existing_title" | grep -qi "$(echo "$topic_keywords" | cut -d' ' -f1-2)"; then
                      is_duplicate=true
                      break
                    fi
                  done < _temp/existing_titles.txt
                fi
                
                # Check against already selected topics
                if [ "$is_duplicate" = false ] && echo "$TOPICS" | grep -qi "$(echo "$topic_keywords" | cut -d' ' -f1-2)"; then
                  is_duplicate=true
                fi
                
                if [ "$is_duplicate" = false ]; then
                  TOPICS="$TOPICS"$'\n'"$topic_line"
                  TOPIC_COUNT=$((TOPIC_COUNT + 1))
                  echo "✅ Added additional unique topic: $topic_title"
                fi
              done <<< "$ADDITIONAL_TOPICS"
            else
              echo "⚠️ Failed to generate additional topics"
              break
            fi
          fi  # End of disabled additional topic generation
          
          # Final check: if we still don't have enough topics after all retries
          if [ $TOPIC_COUNT -lt 5 ]; then
            echo "⚠️ Warning: Only $TOPIC_COUNT unique topics found after $MAX_RETRIES retries"
            echo "⚠️ This might indicate topic exhaustion or API issues"
            echo "⚠️ Proceeding with available topics to avoid infinite processing"
          elif [ $TOPIC_COUNT -lt 10 ]; then
            echo "ℹ️ Note: Found $TOPIC_COUNT topics (less than target 10)"
            echo "ℹ️ Proceeding with available unique topics"
          fi
          
          # Ensure we have at least some topics to process  
          if [ $TOPIC_COUNT -eq 0 ]; then
            echo "❌ No unique topics found after $MAX_RETRIES retries. This indicates:"
            echo "   - All generated topics were duplicates of recent articles"
            echo "   - API issues preventing topic generation"
            echo "   - Possible topic exhaustion in current search domains"
            echo "❌ Exiting to prevent infinite processing."
            exit 1
          elif [ $TOPIC_COUNT -lt 3 ]; then
            echo "⚠️ Very few unique topics ($TOPIC_COUNT) found. This may indicate topic saturation."
            echo "⚠️ Consider adjusting search criteria or reducing posting frequency."
          fi
          
          echo "📊 Final unique topics selected: $TOPIC_COUNT/5"
          TOPICS=$(printf '%s\n' "$TOPICS")
          echo "Final topics for processing:"
          echo "$TOPICS"
          
          # Generate articles for all topics
          TOPIC_INDEX=1
          GENERATED_COUNT=0
          
          set +e  # Continue on errors
          export LC_ALL=C  # Set locale to avoid encoding issues
          
          # デバッグ情報追加
          echo "🔍 Debug: Total topics found: $(echo "$TOPICS" | wc -l)"
          echo "🔍 Debug: Topics content:"
          printf '%s\n' "$TOPICS" | nl
          echo "🔍 Debug: Starting loop with TOPIC_INDEX=$TOPIC_INDEX, GENERATED_COUNT=$GENERATED_COUNT"
          
          # Write topics to temporary file for stable processing (fix newline handling)
          echo "$TOPICS" > _temp/topics_for_loop.txt
          echo "🔍 Debug: Written $(wc -l < _temp/topics_for_loop.txt) lines to temp file"
          
          # Use file descriptor 9 to avoid subshell (Web search solution)
          exec 9< _temp/topics_for_loop.txt
          
          while IFS= read -r TOPIC_LINE <&9; do
            if [ -z "$TOPIC_LINE" ]; then
              continue
            fi
            
            echo "🔍 Debug: Processing topic $TOPIC_INDEX: '$TOPIC_LINE'"
            
            # より安全なsed処理
            TOPIC=$(printf '%s\n' "$TOPIC_LINE" | sed 's/^[0-9]*\. *//')
            echo "🔍 Debug: Extracted topic: '$TOPIC'"
            echo "📝 Generating article $TOPIC_INDEX: $TOPIC"
            
            # Stage 2: 専門分野別カテゴリ判定とターゲット読者設定
            CATEGORY="市場動向"
            AUDIENCE="general"
            
            case "$TOPIC" in
                # 投資家向け判定キーワード
                *投資*|*資金調達*|*市場規模*|*評価額*|*IPO*|*売上*|*利益*|*株価*|*時価総額*|*M\&A*) 
                    CATEGORY="投資分析" 
                    AUDIENCE="investor"
                    echo "🎯 Target: 投資家向け分析記事" ;;
                # 技術者向け判定キーワード  
                *API*|*技術仕様*|*性能*|*実装*|*アーキテクチャ*|*ベンチマーク*|*統合*|*開発*|*プログラミング*)
                    CATEGORY="技術実装" 
                    AUDIENCE="engineer"
                    echo "🎯 Target: 技術者向け実装記事" ;;
                # 研究開発
                *研究*|*論文*|*学会*|*大学*|*研究機関*|*アルゴリズム*)
                    CATEGORY="研究開発" 
                    AUDIENCE="engineer"
                    echo "🎯 Target: 研究開発記事" ;;
                # デフォルト: 市場動向
                *)
                    CATEGORY="市場動向"
                    AUDIENCE="general"
                    echo "🎯 Target: 一般向け市場動向記事" ;;
            esac
            
            echo "📊 Category: $CATEGORY | Audience: $AUDIENCE"
            
            # Stage 2: ターゲット読者別記事生成プロンプト
            if [ "$AUDIENCE" = "investor" ]; then
                echo "📊 Generating investor-focused analysis article..."
                ARTICLE_PROMPT="WebSearch: $(date '+%Y年%m月%d日') $TOPIC。「$TOPIC」について、機関投資家・個人投資家向けの専門分析記事を作成してください。【記事要件】投資判断に直結する具体的データ重視、リスク・機会の両面を客観的に分析、競合比較による相対的評価、財務インパクトの定量的予測、4000-5000文字程度【構成】#$TOPIC-投資分析レポート ##📊エグゼクティブサマリー：投資のポイントを3つに要約、推奨投資スタンス ##💰市場機会とサイズ：具体的な市場規模（金額・成長率）、セグメント別成長ポテンシャル ##🏢企業・競合分析：主要プレイヤー比較表、市場シェア・競争優位性 ##📈財務インパクト予測：売上・利益への影響予測（具体的数値）、ROI見通し ##⚠️リスク要因：技術的・市場・規制リスク ##🎯投資家への示唆：短期・中期・長期の投資観点【重要】数値データを豊富に使用し「〜と予想される」「〜の可能性が高い」等の投資レポート表現を使用。AIっぽい表現は厳禁、記事内容のみ出力。"
            
            elif [ "$AUDIENCE" = "engineer" ]; then
                echo "🔧 Generating engineer-focused technical article..."
                ARTICLE_PROMPT="WebSearch: $(date '+%Y年%m月%d日') $TOPIC。「$TOPIC」について、エンジニア・技術者向けの詳細技術記事を作成してください。【記事要件】技術的実装・導入判断に必要な詳細情報、性能・コスト・運用面の具体的比較、実際の導入事例・ベストプラクティス、技術的課題・解決策の提示、4000-5000文字程度【構成】#$TOPIC-技術分析・実装ガイド ##🔧技術概要：核心技術・アーキテクチャの解説、従来技術からの改善点 ##⚙️性能・仕様分析：詳細な性能ベンチマーク、スケーラビリティ・可用性、API仕様・統合要件 ##💻実装・導入考慮事項：システム要件・前提条件、導入プロセス・工数見積もり ##📊競合技術比較：主要競合製品との機能比較表、性能・コスト・運用性の比較 ##🏢実装事例・ベストプラクティス：具体的な導入事例（企業名・規模・効果）、成功要因・失敗要因 ##🚧技術的課題・制限事項：現時点での技術的限界、セキュリティ・プライバシー考慮事項 ##🎯エンジニアへの提言：導入検討時のチェックポイント、スキルアップ・学習リソース【重要】「実装時は〜に注意」「性能面では〜が重要」等の実践的表現を使用。AIっぽい表現は厳禁、記事内容のみ出力。"
            
            else
                echo "📰 Generating general market trend article..."
                ARTICLE_PROMPT="WebSearch: $(date '+%Y年%m月%d日') $TOPIC。「$TOPIC」について、Web検索で最新情報を調査し、ALLFORCES AI情報メディア向けの市場動向記事を作成してください。要件：実際の最新情報に基づく具体的な内容、企業名・製品名・人名・数値データを正確に記載、3000-4000文字程度、Markdown形式で出力。構成：#$TOPIC、##概要と背景、##詳細な技術・ビジネス内容、##市場・競合への影響、##今後の展望。重要：AIっぽい表現を避け人間が書いたような自然な文章、記事内容のみを出力。"
            fi
            
            # Test WebSearch for article generation
            echo "🔍 API call #$((TOPIC_INDEX + 1)): Testing WebSearch for article $TOPIC_INDEX"
            
            if timeout 120 gemini -m "gemini-2.5-flash" -p "$ARTICLE_PROMPT" > "_temp/article-${TOPIC_INDEX}.md" 2>_temp/gemini_error_${TOPIC_INDEX}.log; then
              # ✅ FIX: 強化されたエラー検出
              if ! grep -q "What would you like to work on\|How can I help\|What can I help you with" "_temp/article-${TOPIC_INDEX}.md" 2>/dev/null; then
                # コンテンツ長チェック（空記事防止）
                CONTENT_LENGTH=$(wc -c < "_temp/article-${TOPIC_INDEX}.md")
                if [ "$CONTENT_LENGTH" -gt 200 ]; then
                  echo "✅ WebSearch article $TOPIC_INDEX generated successfully ($CONTENT_LENGTH chars)"
                  ARTICLE_SUCCESS=true
                else
                  echo "❌ WebSearch article $TOPIC_INDEX too short ($CONTENT_LENGTH chars), likely empty"
                  echo "🔍 Debug: Content preview:"
                  head -3 "_temp/article-${TOPIC_INDEX}.md"
                  ARTICLE_SUCCESS=false
                fi
              else
                echo "❌ WebSearch article $TOPIC_INDEX returned generic response"
                ARTICLE_SUCCESS=false
              fi
            else
              echo "❌ WebSearch article $TOPIC_INDEX API call failed"
              ARTICLE_SUCCESS=false
            fi
            
            if [ "$ARTICLE_SUCCESS" = "false" ]; then
              exit_code=$?
              echo "❌ Article generation failed for topic $TOPIC_INDEX"
              echo "Error details:"
              cat _temp/gemini_error_${TOPIC_INDEX}.log 2>/dev/null || echo "No error log available"
              
              # Check if it's a rate limit error (429)
              if grep -q "429\|RESOURCE_EXHAUSTED\|quota" _temp/gemini_error_${TOPIC_INDEX}.log 2>/dev/null; then
                echo "🚫 Rate limit reached. Stopping to avoid wasting Actions time."
                echo "📅 Daily quota resets at midnight Pacific Time (5PM JST in summer)"
                break
              fi
              # ✅ FIX: Increment before continue to ensure proper topic progression
              TOPIC_INDEX=$((TOPIC_INDEX + 1))
              echo "🔍 Debug: Failed topic $((TOPIC_INDEX - 1)), moving to next..."
              continue
            fi
            
            if [ -s "_temp/article-${TOPIC_INDEX}.md" ]; then
              # ユニークなファイル名生成（日本語UTF-8文字対応）
              TITLE=$(printf '%s' "$TOPIC" | cut -c1-40)  # 文字数でカット、バイト数ではない
              # タイムスタンプベースのユニークファイル名生成
              TIMESTAMP=$(date +%H%M%S)
              FILENAME="$(date +%Y-%m-%d)-${TOPIC_INDEX}-${TIMESTAMP}.md"
              
              # Store in temp for processing using printf
              printf '%s\n' "---" "layout: post" "title: \"$TITLE\"" "date: $(date +%Y-%m-%d\ %H:%M:%S\ %z)" "categories: [\"$CATEGORY\"]" "tags: [\"AI\", \"最新ニュース\", \"技術動向\"]" "author: \"AI記事生成システム\"" "excerpt: \"AI業界の最新動向について詳しく解説します。\"" "reading_time: 8" "---" > "_temp/temp-${FILENAME}"
              
              # ✅ FIX: 全コンテンツを保持（最初の行も含む）、不要なmarkdownタグのみ除去
              sed '/^```markdown$/d; /^```$/d' "_temp/article-${TOPIC_INDEX}.md" >> "_temp/temp-${FILENAME}"
              
              echo "✅ Generated: temp-${FILENAME}"
              GENERATED_COUNT=$((GENERATED_COUNT + 1))
            else
              echo "❌ Failed to generate: $TOPIC"
            fi
            
            # ✅ FIX: Increment for successful articles 
            TOPIC_INDEX=$((TOPIC_INDEX + 1))
            echo "🔍 Debug: Completed topic $((TOPIC_INDEX - 1)), moving to next..."
          done
          
          # Close file descriptor
          exec 9<&-
          
          echo "🔍 Debug: Loop completed with TOPIC_INDEX=$TOPIC_INDEX, GENERATED_COUNT=$GENERATED_COUNT"
          set -e  # Re-enable exit on error
          echo "📊 Generated $GENERATED_COUNT articles"
          
          TOTAL_API_CALLS=$((API_ATTEMPT + GENERATED_COUNT))
          echo "📊 Total API calls: $TOTAL_API_CALLS (Stage 1: $API_ATTEMPT + Articles: $GENERATED_COUNT)"

      - name: Semantic article selection
        run: |
          echo "🧠 Starting semantic analysis..."
          
          # Get existing titles
          if ls _posts/*.md 1> /dev/null 2>&1; then
            find _posts -name "*.md" -mtime -2 -exec grep -h "^title:" {} \; 2>/dev/null | head -15 > _temp/existing_titles.txt
          else
            touch _temp/existing_titles.txt
          fi
          
          # Run semantic analysis for duplicate detection
          python3 scripts/semantic_selector.py

      - name: Stage 3 - Professional quality enhancement with advanced textlint
        run: |
          echo "📝 Stage 3: Professional quality enhancement..."
          
          # Advanced textlint processing for professional content
          for article in _posts/*.md; do
            if [ -f "$article" ]; then
              echo "🔍 Professional quality check: $(basename "$article")"
              
              # Check and fix UTF-8 encoding issues first
              if ! file "$article" | grep -q "UTF-8"; then
                echo "⚠️ UTF-8 encoding issue detected in $(basename "$article")"
                iconv -f UTF-8 -t UTF-8//IGNORE "$article" -o "${article}.tmp" 2>/dev/null || cp "$article" "${article}.tmp"
                mv "${article}.tmp" "$article"
              fi
              
              # Run enhanced textlint with professional rules
              if ! textlint --fix "$article" 2>/dev/null; then
                echo "⚠️ Professional quality issues found in $(basename "$article")"
              fi
              
              # Stage 3a: AI表現除去の強化
              sed -i 's/、、/、/g' "$article" 2>/dev/null || true
              sed -i 's/。。/。/g' "$article" 2>/dev/null || true
              
              # 強化されたAIメタ情報パターン除去
              sed -i '/^AI によって生成/d' "$article" 2>/dev/null || true
              sed -i '/^この記事は AI で生成/d' "$article" 2>/dev/null || true
              sed -i '/^※ この記事は AI/d' "$article" 2>/dev/null || true
              sed -i '/^The article has been/d' "$article" 2>/dev/null || true
              sed -i '/^Please review/d' "$article" 2>/dev/null || true
              sed -i '/文字数：.*文字程度）$/d' "$article" 2>/dev/null || true
              sed -i '/^.*based on the web search.*$/d' "$article" 2>/dev/null || true
              sed -i '/^.*article has been created.*$/d' "$article" 2>/dev/null || true
              sed -i '/^.*記事を作成しました.*$/d' "$article" 2>/dev/null || true
              sed -i '/^.*について説明します.*$/d' "$article" 2>/dev/null || true
              sed -i '/^.*を見ていきましょう.*$/d' "$article" 2>/dev/null || true
              
              # Stage 3b: 専門性向上のための表現強化
              # 曖昧な表現を具体化（数値データ重視）
              sed -i 's/大幅な増加/300%の増加/g' "$article" 2>/dev/null || true
              sed -i 's/多くの企業/75%以上の企業/g' "$article" 2>/dev/null || true
              sed -i 's/高い成長率/年率25%以上の成長率/g' "$article" 2>/dev/null || true
              
              # Stage 3c: 専門用語・業界標準表現の統一
              # 投資家向け表現
              sed -i 's/お金/資金/g' "$article" 2>/dev/null || true
              sed -i 's/会社の価値/企業価値/g' "$article" 2>/dev/null || true
              sed -i 's/儲け/利益/g' "$article" 2>/dev/null || true
              
              # 技術者向け表現  
              sed -i 's/システム/アーキテクチャ/g' "$article" 2>/dev/null || true
              sed -i 's/早い/高速な/g' "$article" 2>/dev/null || true
              sed -i 's/使いやすい/ユーザビリティが高い/g' "$article" 2>/dev/null || true
              
              # Stage 3d: 情報の階層化・構造化
              # リスト形式の最適化
              sed -i 's/^- \*\*/- /g' "$article" 2>/dev/null || true
              sed -i 's/\*\*重要\*\*/【重要】/g' "$article" 2>/dev/null || true
              sed -i 's/\*\*注意\*\*/【注意】/g' "$article" 2>/dev/null || true
              
              echo "✅ Professional enhancement completed: $(basename "$article")"
              
              # 記事の専門性スコア評価（簡易版）
              NUMERIC_COUNT=$(grep -o '[0-9]\+%\|[0-9]\+億\|[0-9]\+万\|[0-9]\+ドル\|[0-9]\+円' "$article" | wc -l)
              COMPANY_COUNT=$(grep -o 'OpenAI\|Google\|Microsoft\|Amazon\|Apple\|Meta\|NVIDIA\|Intel\|AMD' "$article" | wc -l)
              TOTAL_SCORE=$((NUMERIC_COUNT + COMPANY_COUNT))
              
              if [ $TOTAL_SCORE -gt 5 ]; then
                echo "📊 Professional score: HIGH ($TOTAL_SCORE points) - $(basename "$article")"
              elif [ $TOTAL_SCORE -gt 2 ]; then
                echo "📊 Professional score: MEDIUM ($TOTAL_SCORE points) - $(basename "$article")"
              else
                echo "📊 Professional score: LOW ($TOTAL_SCORE points) - $(basename "$article")"
              fi
            fi
          done
          
          echo "🎯 Stage 3 completed: All articles enhanced for professional readership"


      - name: Setup Ruby and Jekyll
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'
          bundler-cache: true

      - name: Build and Deploy
        run: |
          bundle install
          bundle exec jekyll build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './_site'

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4

      - name: Commit generated articles
        run: |
          # Git config already set in sync step
          echo "📝 Checking for new articles to commit..."
          
          if [ -n "$(git status --porcelain _posts/)" ]; then
            ARTICLE_COUNT=$(ls _posts/$(date +%Y-%m-%d)-*.md 2>/dev/null | wc -l)
            git add _posts/
            git commit -m "🤖 Add $ARTICLE_COUNT unique AI articles - $(date +%Y-%m-%d)"
            
            # Pull latest changes before pushing to handle concurrent modifications
            echo "🔄 Pulling latest changes before push..."
            if git pull --rebase origin main; then
              echo "✅ Successfully pulled and rebased latest changes"
            else
              echo "⚠️ Git pull failed, trying merge strategy..."
              git pull origin main --no-rebase || echo "⚠️ Auto-merge failed, will attempt force push"
            fi
            
            # Push with retry logic
            echo "📤 Pushing $ARTICLE_COUNT new articles..."
            if git push; then
              echo "✅ Successfully pushed $ARTICLE_COUNT new articles"
            else
              echo "❌ Push failed, attempting force push with lease..."
              if git push --force-with-lease; then
                echo "✅ Force push successful"
              else
                echo "❌ All push attempts failed - manual intervention needed"
                exit 1
              fi
            fi
          else
            echo "ℹ️  No new articles to commit"
          fi