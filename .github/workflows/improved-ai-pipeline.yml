name: Simple AI Article Pipeline

on:
  schedule:
    - cron: '15 */8 * * *'
  workflow_dispatch:

jobs:
  generate-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4
          pip install transformers torch sentence-transformers scikit-learn numpy

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Install Node dependencies
        run: |
          npm install -g @google/gemini-cli
          mkdir -p _temp

      - name: Generate diverse topics
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          export GEMINI_API_KEY="$GEMINI_API_KEY"
          
          echo "🎯 Generating diverse AI topics..."
          gemini -m "gemini-2.5-flash" --prompt "WebSearch: AI業界 最新ニュース トレンド 2025。以下の条件で多様なAI業界の話題を12個生成してください：
          
          条件：
          1. 異なる分野（医療、金融、製造業、エンターテイメントなど）
          2. 異なる技術（LLM、画像生成、音声、ロボティクスなど）
          3. 具体的な企業名・製品名・数値を含む
          4. 最新の発表・ニュースに基づく
          
          形式（1行ずつ）：
          1. [分野] [企業名] - [具体的内容]
          
          例：
          1. 医療AI Google DeepMind - AlphaFold3が新薬開発を30%加速化
          2. 金融AI JPMorgan - AI投資アルゴリズムが15%のリターン達成
          
          このような具体性のある12の話題をリストしてください。" > _temp/topics.txt
          
          echo "Generated topics:"
          head -15 _temp/topics.txt

      - name: Simple duplicate check and article generation
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          export GEMINI_API_KEY="$GEMINI_API_KEY"
          
          # Simple keyword-based duplicate check (relaxed)
          echo "📝 Starting article generation with relaxed duplicate checking..."
          
          TOPICS=$(grep -E "^[0-9]+\." _temp/topics.txt | head -8)
          echo "Topics to process:"
          echo "$TOPICS"
          
          # Get recent titles for comparison
          EXISTING_TITLES=""
          if ls _posts/*.md 1> /dev/null 2>&1; then
            EXISTING_TITLES=$(find _posts -name "*.md" -mtime -2 -exec grep -h "^title:" {} \; 2>/dev/null | sed 's/title: *//' | sed 's/"//g' | tr '[:upper:]' '[:lower:]' | head -20)
            echo "Recent titles for comparison:"
            echo "$EXISTING_TITLES" | head -5
          fi
          
          ARTICLES_CREATED=0
          MAX_ARTICLES=6
          
          echo "$TOPICS" | while IFS= read -r TOPIC_LINE; do
            if [ "$ARTICLES_CREATED" -ge "$MAX_ARTICLES" ]; then
              echo "✅ Reached maximum articles: $MAX_ARTICLES"
              break
            fi
            
            if [ -z "$TOPIC_LINE" ]; then
              continue
            fi
            
            TOPIC=$(echo "$TOPIC_LINE" | sed 's/^[0-9]*\. *//')
            echo ""
            echo "🔍 Processing: $TOPIC"
            
            # Simple similarity check - only skip if very similar
            SKIP=false
            if [ ! -z "$EXISTING_TITLES" ]; then
              # Extract key terms for comparison
              KEY_TERMS=$(echo "$TOPIC" | tr '[:upper:]' '[:lower:]' | grep -oE '[a-zA-Z0-9ぁ-んァ-ヶ一-龠]{3,}' | head -3)
              
              # Check if multiple key terms exist in recent titles
              MATCH_COUNT=0
              for term in $KEY_TERMS; do
                if echo "$EXISTING_TITLES" | grep -q "$term"; then
                  MATCH_COUNT=$((MATCH_COUNT + 1))
                fi
              done
              
              # Only skip if 2+ key terms match (very strict)
              if [ "$MATCH_COUNT" -ge 2 ]; then
                echo "⚠️  Potential duplicate detected (matched terms: $MATCH_COUNT), skipping"
                SKIP=true
              else
                echo "✅ Unique topic (matched terms: $MATCH_COUNT), proceeding"
              fi
            fi
            
            if [ "$SKIP" = false ]; then
              echo "📝 Generating article for: $TOPIC"
              
              # Determine category
              CATEGORY="最新動向"
              if [[ "$TOPIC" =~ (研究|論文|学会) ]]; then
                CATEGORY="研究論文"
              elif [[ "$TOPIC" =~ (技術|アルゴリズム|仕組み) ]]; then
                CATEGORY="技術解説"  
              elif [[ "$TOPIC" =~ (導入|事例|活用) ]]; then
                CATEGORY="実装事例"
              elif [[ "$TOPIC" =~ (市場|分析|予測) ]]; then
                CATEGORY="業界分析"
              fi
              
              # Generate article
              PROMPT=$(cat <<EOF
WebSearch: $(date '+%Y年%m月%d日') $TOPIC。「$TOPIC」について、Web検索で最新情報を調査し、ALLFORCES AI情報メディア向けの専門記事を作成してください。

要件：
- 実際の最新情報に基づく具体的な内容
- 企業名・製品名・人名・数値データを正確に記載  
- 技術者・投資家向けの詳細な分析
- 3000-4000文字程度
- Markdown形式で出力

構成：
# $TOPIC
## 概要と背景
## 詳細な技術・ビジネス内容
## 市場・競合への影響
## 今後の展望

専門性と信頼性を重視した記事を作成してください。
EOF
)

              gemini -m "gemini-2.5-flash" --prompt "$PROMPT" > "_temp/article-$ARTICLES_CREATED.md"
              
              if [ -s "_temp/article-$ARTICLES_CREATED.md" ]; then
                # Create Jekyll post
                TITLE=$(echo "$TOPIC" | cut -c1-80)
                SLUG=$(echo "$TOPIC" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-zA-Z0-9あ-んア-ン一-龯]/-/g' | sed 's/--*/-/g' | sed 's/^-\|-$//g' | cut -c1-50)
                FILENAME="$(date +%Y-%m-%d)-$((ARTICLES_CREATED + 1))-$SLUG.md"
                
                # Create front matter
                cat > "_posts/$FILENAME" <<EOF
---
layout: post
title: "$TITLE"
date: $(date '+%Y-%m-%d %H:%M:%S %z')
categories: ["$CATEGORY"]
tags: ["AI", "最新ニュース", "技術動向"]
author: "AI記事生成システム"
excerpt: "AI業界の最新動向について詳しく解説します。"
reading_time: 8
---

EOF
                
                # Add article content
                sed '1d' "_temp/article-$ARTICLES_CREATED.md" | sed '/^```markdown$/d' | sed '/^```$/d' >> "_posts/$FILENAME"
                
                echo "✅ Created: $FILENAME"
                ARTICLES_CREATED=$((ARTICLES_CREATED + 1))
              else
                echo "❌ Failed to generate article"
              fi
            fi
          done
          
          echo ""
          echo "📊 Generation completed: $ARTICLES_CREATED articles created"

      - name: Setup Ruby and Jekyll
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'
          bundler-cache: true

      - name: Build and Deploy
        run: |
          bundle install
          bundle exec jekyll build

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './_site'

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4

      - name: Commit generated articles
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          if [ -n "$(git status --porcelain _posts/)" ]; then
            ARTICLE_COUNT=$(ls _posts/$(date +%Y-%m-%d)-*.md 2>/dev/null | wc -l)
            git add _posts/
            git commit -m "🤖 Add $ARTICLE_COUNT AI articles - $(date +%Y-%m-%d)"
            git push
            echo "✅ Committed $ARTICLE_COUNT new articles"
          else
            echo "ℹ️  No new articles to commit"
          fi